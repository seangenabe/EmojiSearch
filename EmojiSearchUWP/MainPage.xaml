<Page
    x:Class="EmojiSearchUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    Loaded="Page_Loaded">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Border BorderBrush="Gray" BorderThickness="2" Grid.Column="0">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBox Text="{Binding Query}" Height="31" Grid.Column="0" VerticalAlignment="Center" BorderThickness="0"/>
          <TextBlock Text="&#xE11A;" FontFamily="Segoe UI Symbol" Grid.Column="1" VerticalAlignment="Center"/>
        </Grid>
      </Border>
      <Grid Grid.Column="1">
        <Button >ssdf</Button>
      </Grid>
    </Grid>
    <Grid Grid.Row="1">
      <ListView ItemsSource="{Binding Emojis}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding Value}" FontFamily="Segoe UI Emoji" FontSize="18"/>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </Grid>
  </Grid>
</Page>
